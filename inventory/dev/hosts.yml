---
all:
  children:
    linux_servers:
      children:
        ansible_control_nodes:
          hosts:
            azdo-controlnode:

    windows_servers:
      children:
        domain_controllers:
          hosts:
            azdo-adserver:
        app:
          hosts:
            appserver01:
              role: azdo_first
            appserver02:
              role: azdo_additional
        database_servers:
          hosts:
            azdo-sqlserver:
